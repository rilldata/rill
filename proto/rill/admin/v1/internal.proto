syntax = "proto3";
package rill.admin.v1;

message PageToken {
    // cursor is kept as an array since it may not be possible to use one field to unqiuely identify in all cases
    repeated string cursor = 1;
}