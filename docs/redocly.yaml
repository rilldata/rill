# https://redocly.com/docs/cli/configuration

extends:
  - recommended

decorators:
  remove-unused-components: on
  remove-unused-tags: on

openapi:
  schemasExpansionLevel: 2
  showExtensions: true

# Point to the filtered public API spec
apis:
  public@v1:
    root: api/openapi.yaml
