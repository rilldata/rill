---
title: Supabase
description: Connect ClickHouse to data in Supabase
sidebar_label: Supabase
sidebar_position: 130
---

import WrongOLAP from '@site/src/components/WrongOLAP';
import ClickHousePrereq from '@site/src/components/ClickHousePrereq';

<WrongOLAP engine="clickhouse" />

## Overview

[Supabase](https://supabase.com/) is an open-source Firebase alternative built on PostgreSQL. Since Supabase uses PostgreSQL under the hood, ClickHouse connects to it via the [`postgresql()` table function](https://clickhouse.com/docs/en/sql-reference/table-functions/postgresql).

<ClickHousePrereq />

## Credentials

You can find your Supabase connection details in the Supabase dashboard under **Settings > Database**. Add them to your project's `.env` file:

```bash
SUPABASE_HOST=db.your-project-ref.supabase.co
SUPABASE_PORT=5432
SUPABASE_DATABASE=postgres
SUPABASE_USER=postgres
SUPABASE_PASSWORD=your_password
```

For details on managing credentials, see [Configure Local Credentials](/developers/build/connectors/credentials).

## Model Configuration

```yaml
type: model
connector: my_clickhouse

sql: |
  SELECT *
  FROM postgresql(
    '{{ .env.SUPABASE_HOST }}:{{ .env.SUPABASE_PORT }}',
    '{{ .env.SUPABASE_DATABASE }}',
    'my_table',
    '{{ .env.SUPABASE_USER }}',
    '{{ .env.SUPABASE_PASSWORD }}'
  )
```

### Querying a Specific Schema

Supabase organizes data into schemas (e.g., `public`, `auth`). To query a table in a specific schema:

```yaml
sql: |
  SELECT *
  FROM postgresql(
    '{{ .env.SUPABASE_HOST }}:{{ .env.SUPABASE_PORT }}',
    '{{ .env.SUPABASE_DATABASE }}',
    'my_table',
    '{{ .env.SUPABASE_USER }}',
    '{{ .env.SUPABASE_PASSWORD }}',
    'public'
  )
```

:::tip
Make sure your Supabase project allows connections from your ClickHouse instance's IP address. You may need to adjust your Supabase [network restrictions](https://supabase.com/docs/guides/platform/network-restrictions).
:::

## Reference

For Supabase authentication setup, see the [DuckDB Supabase connector guide](/developers/build/connectors/data-source/duckdb/supabase). See also the [ClickHouse PostgreSQL documentation](https://clickhouse.com/docs/en/sql-reference/table-functions/postgresql) for full syntax details.
