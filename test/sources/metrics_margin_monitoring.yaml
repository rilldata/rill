# Model YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/models

type: model
materialize: true

connector: duckdb
# create_secrets_from_connectors: gcs

sql: |
  select * from read_parquet('gs://rilldata-public/metrics_margin_monitoring.parquet')
