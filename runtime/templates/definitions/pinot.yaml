name: pinot
display_name: "Pinot"
driver: pinot
tags: ["pinot", "olap", "analytics", "connector"]
files:
  - name: connector
    path_template: "connectors/[[ .connector_name ]].yaml"
    code_template: |
      # Connector YAML
      # Reference documentation: https://docs.rilldata.com/developers/build/connectors/olap/pinot
      type: connector
      driver: pinot
      [[ renderProps .props ]]
