name: azure-duckdb
display_name: "Azure (DuckDB)"
driver: azure
olap: duckdb
tags: ["azure", "microsoft", "object-storage", "duckdb", "source"]
files:
  - name: model
    path_template: "models/[[ .model_name ]].yaml"
    code_template: |
      # Model YAML
      # Reference documentation: https://docs.rilldata.com/developers/build/connectors/data-source/azure
      type: model
      connector: duckdb
      [[ if .create_secrets_from_connectors -]]
      create_secrets_from_connectors: "[[ .create_secrets_from_connectors ]]"
      [[ end -]]
      sql: |
        [[ .sql ]]
