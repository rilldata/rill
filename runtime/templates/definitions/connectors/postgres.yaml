name: postgres
display_name: "Postgres"
driver: postgres
tags: ["postgres", "postgresql", "database", "sql-store", "source", "connector"]
files:
  - name: connector
    path_template: "connectors/[[ .connector_name ]].yaml"
    code_template: |
      # Connector YAML
      # Reference documentation: https://docs.rilldata.com/developers/build/connectors/data-source/postgres
      type: connector
      driver: postgres
      [[ renderProps .props ]]
