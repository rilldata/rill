name: s3
display_name: "Amazon S3"
driver: s3
tags: ["s3", "aws", "object-storage", "source", "connector"]
files:
  - name: connector
    path_template: "connectors/[[ .connector_name ]].yaml"
    code_template: |
      # Connector YAML
      # Reference documentation: https://docs.rilldata.com/developers/build/connectors/data-source/s3
      type: connector
      driver: s3
      [[ renderProps .props ]]
