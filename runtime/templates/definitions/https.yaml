name: https
display_name: "HTTPS"
driver: https
tags: ["https", "http", "url", "file-store", "source", "connector"]
files:
  - name: connector
    path_template: "connectors/[[ .connector_name ]].yaml"
    code_template: |
      # Connector YAML
      # Reference documentation: https://docs.rilldata.com/developers/build/connect/#adding-a-remote-source
      type: connector
      driver: https
      [[ renderProps .props ]]
