package metricsview

// func (a *AST) SQLForNativePivot(cellLimit int) (string, []any, error) {
// // D with
// //       raw as (select * from foo),
// //       data as (select * from raw where if(exists (select count(*) count from raw having count > 5), error('too many cells'), true)),
// //       res as (pivot data on a using sum(b)) select * from res;
// }

// func (a *AST) sqlForTablePivot(table string) (string, []any, error) {

// }
